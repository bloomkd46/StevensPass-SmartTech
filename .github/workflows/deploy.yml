name: Deploy Website

on:
  # Runs on pushes targeting the default branch
  push:
    branches:
      - main
      - beta

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:


# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  BUILD_PATH: "." # default value when not using subfolders
  YARN_IGNORE_ENGINES: true
  TZ: America/Los_Angeles
  # BUILD_PATH: subfolder
  SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
  SENTRY_ORG: stevens-pass-smart-tech
  SENTRY_PROJECT: stevens-pass-smart-tech


jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      changes: ${{ steps.build.outputs.changes }}
      indexnow_curl: ${{ steps.build.outputs.indexnow_curl }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Find last successful deploy run with artifact
        id: last_success
        run: |
          runs=$(gh run list --workflow="Deploy Website" --branch="$GITHUB_REF_NAME" --json databaseId,number,status,conclusion,createdAt --limit 20 | jq '[.[] | select(.status=="completed" and .conclusion=="success" and .databaseId != '${{ github.run_id }}')]')
          found=""
          for row in $(echo "${runs}" | jq -r '.[] | @base64'); do
            _jq() { echo ${row} | base64 --decode | jq -r ${1}; }
            run_id=$(_jq '.databaseId')
            run_number=$(_jq '.number')
            # Check if the artifact exists for this run
            if gh api /repos/${{ github.repository }}/actions/runs/$run_id/artifacts | jq -e '.artifacts[] | select(.name=="website")' > /dev/null; then
              echo "::notice::Using artifact from run #$run_number (id $run_id)"
              echo "run_id=$run_id" >> $GITHUB_OUTPUT
              echo "run_number=$run_number" >> $GITHUB_OUTPUT
              found="yes"
              break
            else
              echo "::warning::Run #$run_number (id $run_id) does not have a website artifact, skipping."
            fi
          done
          if [ -z "$found" ]; then
            echo "run_id=" >> $GITHUB_OUTPUT
            echo "run_number=" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download previous deploy artifact
        if: steps.last_success.outputs.run_id != ''
        uses: actions/download-artifact@v4
        with:
          name: website
          path: previous-deploy/browser
          run-id: ${{ steps.last_success.outputs.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: yarn install

      - name: Build Project
        id: build
        run: yarn deploy
        timeout-minutes: 10
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Sentry Sourcemaps
        run: yarn sentry:sourcemaps


      - name: Upload website
        uses: actions/upload-artifact@v4
        with:
          path: ./dist/stevens-pass-smart-tech/browser
          name: website

  deploy_firebase:
    name: Deploy to Firebase
    runs-on: ubuntu-latest
    needs: build
    env:
      FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        sparse-checkout: |
          firebase.json
          .firebaserc
          .nvmrc
        sparse-checkout-cone-mode: false

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version-file: .nvmrc

    - name: Install Firebase
      run: npm install -g firebase-tools@^14.3.1

    - name: Download website
      uses: actions/download-artifact@v4
      with:
        name: website
        path: ./dist/stevens-pass-smart-tech/browser

    - name: Deploy to Firebase
      run: |
        firebase deploy --project "stevenspass-smarttech" -m "Deployed via GitHub Actions ${{github.event_name}} Run #${{ github.run_number }} - https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

    - name: Get the current version from package.json
      id: release
      run: echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

    - name: Create Sentry release
      uses: getsentry/action-release@v3
      with:
        environment: 'production'
        release: SmartTech@${{ steps.release.outputs.version }}
        set_commits: 'manual'
        commit: ${{ github.sha }}
        previous_commit: ${{ github.event.before }}
        repo: StevensPass/SmartTech


  notify_changes:
    name: Notify Changes
    runs-on: ubuntu-latest
    needs:
      - build
      - deploy_firebase
    if: needs.build.outputs.changes == 'true'
    steps:
      - name: Notify Changes
        run: curl ${{ needs.build.outputs.indexnow_curl }}
